import { createSlice } from "@/shared/reduxImports"

const initialState = {}

export const statisticTablesSlice = createSlice({
  name: "statisticTables",
  initialState,
  reducers: {},
})

export const {} = statisticTablesSlice.actions

export default statisticTablesSlice.reducer
